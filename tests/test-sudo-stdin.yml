project:
  name: "test-sudo-stdin"

actions:
  - name: "sudo-stdin"
    run: |
      echo "Testing sudo with stdin..."
      echo "password" | sudo -S ls /root
      echo "Sudo with stdin completed!"

stages:
  test-sudo-stdin:
    steps:
      - action: "sudo-stdin"

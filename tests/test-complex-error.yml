project:
  name: test-complex-error
  version: v0.1.0

actions:
  - name: complex-failing-command
    run: |
      echo "Starting complex operation..."
      for i in $(seq 1 3); do
        echo "Step $i of 3"
        sleep 1
      done
      echo "This will fail now"
      exit 42

stages:
  test-complex-error:
    steps:
      - action: complex-failing-command
project:
  name: test-error
  version: v0.1.0

actions:
  - name: failing-command
    run: |
      echo "This command will fail"
      exit 1

stages:
  test-error:
    steps:
      - action: failing-command